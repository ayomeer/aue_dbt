dbt_biotope:
  target: dev
  outputs:
    dev:
      dbname: test-db
      host: localhost
      pass: "{{ env_var('DB_PASSWORD') }}" # export DB_PASSWORD='...' in bashrc
      port: 5432
      schema: dbt_biotope
      threads: 1
      type: postgres
      user: postgres