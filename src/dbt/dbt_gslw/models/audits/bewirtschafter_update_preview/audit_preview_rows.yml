models:
  - name: audit_preview_rows
    description: "Row changes comparing the preview of what the table `bewirtschafter` will look like after running the job `transformations.bewirtschafter_update` when run with `--vars 'enable_transfer: true'` with the current `bewirtschafter` table."
